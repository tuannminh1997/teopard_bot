Teopard liquidity fix - 2026-07-05

Files changed:
- analyze.py
- analyze_system_prompt.txt

Main changes:
1. Reworked liquidity zone calculation so near/main/deep are selected by trading-quality signals, not only by nearest pivot inside a time window.
2. Added liquidity point collection from:
   - pivot high/low
   - equal high/low touches
   - wick sweep candles
   - window extreme fallback
3. Added scoring by:
   - number of touches
   - wick sweep count
   - average volume ratio on touches
   - recency
   - distance by ATR according to role near/main/deep
4. No fake separation: if near/main/deep are genuinely the same cluster, output keeps them and adds an overlap note.
5. Added metadata in the Python context:
   - chạm
   - quét
   - vol
   - ATR distance
   - đang chạm giá
   - fallback cực trị
6. Prevented stale fallback highs/lows from being used on the wrong side after price has already broken them.
7. Updated prompt so the model understands overlapping liquidity zones as one cluster and does not treat a zone currently touching price as a far TP/deep target.

Validation:
- analyze.py passed python -m py_compile.
- Liquidity formatter was smoke-tested with synthetic OHLCV data.
